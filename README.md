First to build the application we have to where docker-compose file have

then have to up docker compose with command > docker-compose up

here three container will execute Redis,Mongo and docker_redis_mongo_app
which this application container name

once it up we can verify  with localhost:8080 from any browser i mention below
http://localhost:8080/
